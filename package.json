{
  "name": "noursky-timeclock",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "cd web && npm install && npm run build",
    "test:smoke": "node scripts/smoke-test.mjs"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "hono": "^4.6.0",
    "mysql2": "^3.11.0"
  },
  "devDependencies": {
    "crypto-js": "^4.2.0"
  }
}
